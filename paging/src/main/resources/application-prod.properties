## Production profile overrides
# Enable Flyway and ensure Hibernate does not try to modify the schema
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.jpa.hibernate.ddl-auto=none
