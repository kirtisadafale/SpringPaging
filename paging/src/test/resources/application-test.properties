# Test properties for integration tests that require Redis
# Enable the Redis-backed rate limiter for tests that validate the Lua script and redis interactions
app.rateLimiter.type=redis

# Keep a reasonably small rate limit for test determinism
app.rateLimit.requests=5
app.rateLimit.windowSeconds=60
