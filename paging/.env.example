# Copy this file to .env and fill in real values. Do NOT commit .env with real secrets to source control.
# Example .env for docker-compose local development

# MySQL
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=paging_db
MYSQL_USER=paging_user
MYSQL_PASSWORD=paging_pass

# Spring datasource override (optional)
#SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/paging_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#SPRING_DATASOURCE_USERNAME=${MYSQL_USER}
#SPRING_DATASOURCE_PASSWORD=${MYSQL_PASSWORD}

# Note: you can also set SPRING_REDIS_HOST and SPRING_REDIS_PORT if you need to override
#SPRING_REDIS_HOST=redis
#SPRING_REDIS_PORT=6379
